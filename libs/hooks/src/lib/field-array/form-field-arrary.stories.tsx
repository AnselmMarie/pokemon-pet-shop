import type { Meta } from '@storybook/react';

// import UiTemplate from './global.view';

const Story: Meta = {
  title: 'Hooks/WIP/Form Field Array',
};
export default Story;

export const Primary = {
  render: () => <div>Adding example of hooks</div>,
  args: {},
};
