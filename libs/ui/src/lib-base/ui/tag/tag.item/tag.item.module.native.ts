import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  tag: {
    borderRadius: 100,
    paddingTop: 10,
    paddingBottom: 10,
    paddingLeft: 15,
    paddingRight: 15,
  },
});
