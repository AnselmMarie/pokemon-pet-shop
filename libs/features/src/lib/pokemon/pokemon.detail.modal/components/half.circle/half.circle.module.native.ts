import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  contentCurve: {
    marginTop: -60,
    position: 'absolute',
    bottom: 0,
    left: 0,
    right: 0,
    width: '100%',
    height: 60,
  },
});
